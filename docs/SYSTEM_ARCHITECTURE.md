# 암호화폐 자동 트레이딩 시스템 아키텍처

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [전체 아키텍처](#전체-아키텍처)
3. [서비스 구성](#서비스-구성)
4. [데이터 흐름](#데이터-흐름)
5. [인프라 구성](#인프라-구성)
6. [핵심 전략](#핵심-전략)
7. [배포 및 운영](#배포-및-운영)
8. [모니터링](#모니터링)

---

## 시스템 개요

### 목적
Bybit 거래소에서 USDT 무기한 선물을 대상으로 기술적 분석 기반 자동 트레이딩을 수행하는 분산 시스템

### 핵심 특징
- **분산 마이크로서비스 아키텍처**: 5개의 독립적인 서비스로 구성
- **이벤트 기반 처리**: RabbitMQ를 통한 비동기 메시지 처리
- **확장 가능한 인프라**: AWS ECS Fargate 기반 컨테이너 오케스트레이션
- **실시간 데이터 처리**: 5초~60초 주기의 실시간 시장 분석
- **멀티 타임프레임 분석**: 1분~240분봉 동시 분석
- **리스크 관리**: 고정 포지션 크기($100), 레버리지(10x), 손익비(2:1)

### 기술 스택

- **언어**: Python 3.11
- **클라우드**: AWS (ECS, DynamoDB, MQ, ECR, Secrets Manager)
- **메시지 브로커**: Amazon MQ (RabbitMQ)
- **데이터베이스**: DynamoDB (NoSQL)
- **컨테이너**: Docker
- **IaC**: Terraform
- **거래소 API**: Bybit Unified Trading API (pybit)

---

## 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                          Bybit Exchange API                          │
│                    (USDT Perpetual Futures)                          │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ Market Data / Order Execution
                             │
┌────────────────────────────┴────────────────────────────────────────┐
│                        AWS Cloud Infrastructure                      │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              EventBridge (CloudWatch Events)                 │   │
│  │                                                               │   │
│  │  ┌──────────────────┐         ┌──────────────────┐          │   │
│  │  │ Scanner Schedule │         │ Selector Schedule│          │   │
│  │  │  (1시간마다)      │         │   (1분마다)       │          │   │
│  │  └────────┬─────────┘         └────────┬─────────┘          │   │
│  └───────────┼──────────────────────────────┼──────────────────┘   │
│              │ ECS RunTask                  │ ECS RunTask          │
│              ▼                              ▼                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    ECS Fargate Cluster                       │   │
│  │                 (crypto-backtest-cluster)                    │   │
│  │                                                               │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │   │
│  │  │ Scanner  │  │ Analyzer │  │ Selector │  │  Finder  │   │   │
│  │  │  Task    │  │ Service  │  │  Task    │  │ Service  │   │   │
│  │  │(1시간마다)│  │ (1~10개) │  │ (1분마다)│  │ (1~5개)  │   │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │   │
│  │       │             │             │             │           │   │
│  │       └─────────────┴─────────────┴─────────────┘           │   │
│  │                          │                                   │   │
│  │                          ▼                                   │   │
│  │                  ┌──────────────┐                           │   │
│  │                  │   Executor   │                           │   │
│  │                  │   Service    │                           │   │
│  │                  │    (1개)     │                           │   │
│  │                  └──────┬───────┘                           │   │
│  └─────────────────────────┼───────────────────────────────────┘   │
│                            │                                        │
│  ┌─────────────────────────┼───────────────────────────────────┐   │
│  │         Message Queue (Amazon MQ - RabbitMQ)                │   │
│  │                                                               │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │   │
│  │  │ scan-results │  │ backtest-    │  │ trading-     │      │   │
│  │  │    Queue     │  │ results      │  │ signals      │      │   │
│  │  │              │  │    Queue     │  │    Queue     │      │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘      │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │              DynamoDB Tables (NoSQL Database)                 │   │
│  │                                                               │   │
│  │  ┌──────────────────┐  ┌──────────────────┐                 │   │
│  │  │ crypto-backtest- │  │ crypto-backtest- │                 │   │
│  │  │ scan-results     │  │ backtest-results │                 │   │
│  │  │                  │  │                  │                 │   │
│  │  │ PK: scan_id      │  │ PK: symbol       │                 │   │
│  │  │ SK: symbol       │  │ SK: backtest_id  │                 │   │
│  │  └──────────────────┘  └──────────────────┘                 │   │
│  │                                                               │   │
│  │  ┌──────────────────────────────────────────┐               │   │
│  │  │ crypto-backtest-trading-positions        │               │   │
│  │  │                                          │               │   │
│  │  │ PK: symbol                               │               │   │
│  │  │ SK: signal_timestamp                     │               │   │
│  │  │ GSI: StatusIndex (status)                │               │   │
│  │  │ TTL: 5분 (자동 삭제)                     │               │   │
│  │  └──────────────────────────────────────────┘               │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │              AWS Secrets Manager                              │   │
│  │                                                               │   │
│  │  - crypto-trading/bybit-api (API Key/Secret)                 │   │
│  │  - crypto-trading/rabbitmq (RabbitMQ Credentials)            │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │              Amazon ECR (Container Registry)                  │   │
│  │                                                               │   │
│  │  - scanner-service:latest                                    │   │
│  │  - analyzer-service:latest                                   │   │
│  │  - selector-service:latest                                   │   │
│  │  - finder-service:latest                                     │   │
│  │  - executor-service:latest                                   │   │
│  └───────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 서비스 구성

### 1. Scanner Service (스캐너)
**역할**: 거래 가능한 코인 스캔 및 필터링

**실행 방식**: EventBridge 스케줄 (1시간마다)
**인스턴스 수**: 1개 (태스크 실행)
**CPU/메모리**: 512 CPU / 1024 MB

**주요 기능**:

- Bybit에서 모든 USDT 무기한 선물 목록 조회
- 거래량 필터링 (24시간 거래량 > $1M)
- 변동성 필터링 (24시간 변동성 > 2%)
- 스테이블코인 제외 (USDC, BUSD, DAI 등)
- 스캔 결과를 DynamoDB에 저장
- RabbitMQ `scan-results` 큐에 메시지 발행

**출력 데이터**:
```json
{
  "scan_id": "scan-20251217-093000",
  "symbol": "BTCUSDT",
  "price": 86500.0,
  "volume_24h": 15000000000.0,
  "price_change_24h": 3.5,
  "timestamp": "2025-12-17T09:30:00Z"
}
```

**환경 변수**:
- `MIN_VOLUME_24H`: 1000000 (달러)
- `MIN_VOLATILITY`: 2.0 (%)
- `RABBITMQ_QUEUE`: backtest-tasks

**실행 방식**:
- EventBridge가 1시간마다 ECS Task로 실행
- 1회 실행 후 종료 (Run-to-completion)
- 다음 스케줄까지 대기 (비용 절감)

**코드 위치**:
- `services/scanner/scanner_service.py`
- `services/scanner/Dockerfile`

---

### 2. Analyzer Service (분석기)
**역할**: 백테스팅을 통한 전략 성과 분석

**실행 방식**: ECS Service (상시 실행) + RabbitMQ 메시지 소비
**인스턴스 수**: 1~10개 (Auto Scaling, CPU 70% 기준)
**CPU/메모리**: 1024 CPU / 2048 MB

**주요 기능**:
- Scanner에서 받은 코인에 대해 백테스팅 수행
- 멀티 타임프레임 분석 (1m, 3m, 5m, 15m, 30m, 60m, 120m, 240m)
- 각 타임프레임별 전략 성과 계산
- 1000개 봉 데이터로 과거 성과 분석
- 승률, 손익비, 총 수익률, 최대 낙폭 계산
- 백테스팅 결과를 DynamoDB에 저장
- 성과가 좋은 전략을 RabbitMQ `backtest-results` 큐에 발행

**백테스팅 기간**:
- 1~5분봉: 최근 4일
- 15분봉: 최근 11일
- 30~60분봉: 최근 21일
- 120~240분봉: 최근 42일

**전략 선택 기준**:
- 최소 거래 횟수: 5회
- 최소 승률: 50%
- 최소 손익비: 1.5:1
- 최대 낙폭: -20% 이내

**출력 데이터**:
```json
{
  "symbol": "BTCUSDT",
  "timeframe": "15m",
  "strategy": "fibonacci_bollinger",
  "total_trades": 12,
  "win_rate": 66.7,
  "profit_factor": 2.3,
  "total_return": 15.2,
  "max_drawdown": -8.5,
  "sharpe_ratio": 1.8
}
```

---

### 3. Selector Service (선택기)
**역할**: 백테스팅 결과 기반 최적 전략 선택

**실행 방식**: EventBridge 스케줄 (1분마다)
**인스턴스 수**: 1개 (태스크 실행)
**CPU/메모리**: 256 CPU / 512 MB

**주요 기능**:
- Analyzer에서 받은 백테스팅 결과 평가
- 코인별 최적 타임프레임 선택
- 성과 점수 계산 (승률 × 손익비 × 거래 횟수)
- 선택된 전략을 RabbitMQ `trading-signals` 큐에 발행

**선택 로직**:
```python
score = win_rate * profit_factor * log(total_trades + 1)
```

**필터링 조건**:
- 승률 ≥ 50%
- 손익비 ≥ 1.5
- 거래 횟수 ≥ 5
- 최대 낙폭 ≤ -20%

**출력 데이터**:
```json
{
  "symbol": "BTCUSDT",
  "timeframe": "15m",
  "strategy": "fibonacci_bollinger",
  "score": 45.2,
  "scan_id": "scan-20251217-093000"
}
```

**실행 방식**:
- EventBridge가 1분마다 ECS Task로 실행
- DynamoDB에서 최근 백테스팅 결과 조회
- 최적 전략 선택 후 RabbitMQ에 발행
- 1회 실행 후 종료 (Run-to-completion)

**코드 위치**:
- `services/selector/selector_service.py`
- `services/selector/Dockerfile`

---

### 4. Finder Service (탐색기)
**역할**: 실시간 진입 신호 탐색

**실행 방식**: ECS Service (상시 실행) + RabbitMQ 메시지 소비
**인스턴스 수**: 1~5개 (Auto Scaling, CPU 70% 기준)
**CPU/메모리**: 512 CPU / 1024 MB

**주요 기능**:
- Selector에서 받은 전략으로 현재 시점의 진입 신호 분석
- 200개 최신 봉 데이터로 실시간 분석
- 멀티 타임프레임 피보나치 레벨 계산
- 진입가, 손절가, 익절가 계산
- 심볼별 tickSize/qtyStep에 맞는 정확한 소수점 처리
- 중복 포지션 확인 (이미 진입 중이거나 실행 중인 포지션 스킵)
- 진입 신호를 DynamoDB `trading-positions` 테이블에 저장

**진입 조건**:
- RSI: 30~70 범위
- 볼린저 밴드: 하단/상단 근처
- 피보나치: 주요 레벨 근처 (±2%)
- BTC 추세: 포지션 방향과 일치
- 펀딩비: 포지션 방향과 반대 (역추세)
- 변동성: 50% 이하

**신뢰도 계산**:

```python
confidence = 60  # 기본 점수

# RSI 극단값 (+10점)
if (position_type == 'LONG' and rsi < 35) or (position_type == 'SHORT' and rsi > 65):
    confidence += 10

# 피보나치 레벨 근처 (+10점)
if near_fibonacci_level:
    confidence += 10

# BTC 추세 일치 (+10점)
if btc_trend_matches:
    confidence += 10

# 펀딩비 역추세 (+5점)
if funding_rate_opposite:
    confidence += 5
```

**출력 데이터** (DynamoDB):
```json
{
  "symbol": "BTCUSDT",
  "signal_timestamp": 1702809600,
  "ttl": 1702809900,
  "strategy": "fibonacci_bollinger",
  "timeframe": "15m",
  "confidence": 85,
  "position_type": "LONG",
  "entry_price": 86500.00,
  "stop_loss": 85800.00,
  "take_profit": 87900.00,
  "position_size": 100.0,
  "leverage": 10,
  "rsi": 32.5,
  "bb_position": -0.85,
  "btc_trend": "BULLISH",
  "funding_rate": -0.0001,
  "risk_reward_ratio": 2.0,
  "status": "active",
  "created_at": "2025-12-17T09:30:00Z"
}
```

**중복 진입 방지**:
- 최근 5분 이내 동일 심볼의 포지션 확인
- `executing` 상태면 스킵 (이미 진입 중)
- `active` 상태면 가격/타입 비교 후 유사하면 스킵

**코드 위치**:
- `services/finder/position_finder_service.py`
- `services/finder/Dockerfile`
- `src/strategies/entry_strategy.py`
- `src/utils/indicators.py`

---

### 5. Executor Service (실행기)
**역할**: 실제 주문 실행

**실행 방식**: ECS Service (상시 실행) + 5초 주기 DynamoDB 스캔
**인스턴스 수**: 1개 (고정)
**CPU/메모리**: 256 CPU / 512 MB

**주요 기능**:
- DynamoDB `trading-positions` 테이블에서 `active` 상태 포지션 조회
- 진입 조건 확인 (가격, 신뢰도, 스프레드, 거래량)
- 계정 잔고 및 마진 확인
- 이미 오픈된 포지션 확인 (중복 진입 방지)
- Bybit API를 통한 실제 주문 실행
- 레버리지 설정 (10x)
- Market Order + TP/SL 동시 설정
- 주문 체결 후 상태를 `executing`으로 업데이트

**진입 조건**:
- **신뢰도**: ≥ 60점 (기본 전략 허용)
- **가격 범위**: 진입가 ±0.5% 이내
- **스프레드**: 0.1% 이내
- **거래량**: 24시간 거래량 > 1000
- **잔고**: 사용 가능 마진 ≥ $10 (필요 마진)
- **중복 확인**: 동일 심볼/방향의 오픈 포지션 없음

**주문 수량 계산**:
```python
# 포지션 크기: $100, 레버리지: 10x
raw_qty = (position_size * leverage) / entry_price
# 예: ($100 × 10) / $86,500 = 0.0115 BTC

# qtyStep에 맞춰 반올림
qty = round(raw_qty / qty_step) * qty_step
# 예: 0.0115 → 0.011 (qtyStep = 0.001)
```

**레버리지 설정**:
- 110043 에러 무시 (이미 설정됨)
- 설정 실패해도 주문 계속 진행

**주문 실행**:
```python
order = session.place_order(
    category="linear",
    symbol=symbol,
    side="Buy" or "Sell",
    orderType="Market",
    qty=calculated_qty,
    stopLoss=stop_loss_price,
    takeProfit=take_profit_price,
    positionIdx=0  # One-way mode
)
```

**상태 업데이트**:
- `active` → `executing` (주문 체결 완료)
- DynamoDB에 `order_id`, `executed_at`, `executed_price` 저장

**코드 위치**:
- `services/executor/order_executor_service.py`
- `services/executor/Dockerfile`
- `src/utils/bybit_client.py`

---

## 데이터 흐름

### 전체 파이프라인

```
1. EventBridge (1시간마다)
   → Scanner Task 실행
   ↓
   [RabbitMQ: scan-results Queue]
   ↓
2. Analyzer Service (상시 실행, 1~10개 Auto Scaling)
   - 메시지 소비 및 백테스팅
   - 각 코인 × 8개 타임프레임 분석
   ↓
   [DynamoDB: backtest-results 저장]
   ↓
3. EventBridge (1분마다)
   → Selector Task 실행
   - DynamoDB에서 최적 전략 선택
   ↓
   [RabbitMQ: trading-signals Queue]
   ↓
4. Finder Service (상시 실행, 1~5개 Auto Scaling)
   - 메시지 소비 및 실시간 진입 신호 탐색
   ↓
   [DynamoDB: trading-positions 저장]
   ↓
5. Executor Service (상시 실행, 1개 고정, 5초 주기 스캔)
   - DynamoDB에서 active 포지션 조회
   - 진입 조건 확인
   - 실제 주문 실행
   ↓
   [Bybit Exchange]
```

**실행 방식 비교**:
| 서비스 | 실행 방식 | 트리거 | 장점 |
|--------|-----------|--------|------|
| Scanner | EventBridge Task | 1시간마다 | 비용 효율적, 정확한 스케줄 |
| Analyzer | ECS Service | RabbitMQ 메시지 | 병렬 처리, Auto Scaling |
| Selector | EventBridge Task | 1분마다 | 비용 효율적, 정확한 스케줄 |
| Finder | ECS Service | RabbitMQ 메시지 | 병렬 처리, Auto Scaling |
| Executor | ECS Service | 5초 주기 폴링 | 실시간 처리, 중복 방지 |

### 메시지 흐름 상세

#### 1단계: Scanner → Analyzer
**Queue**: `scan-results`
**메시지 형식**:
```json
{
  "scan_id": "scan-20251217-093000",
  "symbol": "BTCUSDT",
  "price": 86500.0,
  "volume_24h": 15000000000.0,
  "timestamp": "2025-12-17T09:30:00Z"
}
```
**처리 방식**: 
- Scanner가 19개 코인 스캔 → 19개 메시지 발행
- Analyzer 4개가 병렬로 소비 (각각 4~5개씩 처리)

#### 2단계: Analyzer → Selector
**Queue**: `backtest-results`
**메시지 형식**:
```json
{
  "symbol": "BTCUSDT",
  "timeframe": "15m",
  "strategy": "fibonacci_bollinger",
  "total_trades": 12,
  "win_rate": 66.7,
  "profit_factor": 2.3,
  "scan_id": "scan-20251217-093000"
}
```
**처리 방식**:
- 각 코인당 8개 타임프레임 분석 → 최대 152개 메시지 (19×8)
- 성과 기준 통과한 전략만 발행 (실제로는 ~30개)

#### 3단계: Selector → Finder
**Queue**: `trading-signals`
**메시지 형식**:
```json
{
  "symbol": "BTCUSDT",
  "timeframe": "15m",
  "strategy": "fibonacci_bollinger",
  "scan_id": "scan-20251217-093000"
}
```
**처리 방식**:
- 코인별 최적 전략 1개만 선택 → 최대 19개 메시지
- Finder 3개가 병렬로 소비

#### 4단계: Finder → DynamoDB
**Table**: `crypto-backtest-trading-positions`
**데이터 형식**: 위 Finder 섹션 참조
**처리 방식**:
- 진입 신호 발견 시 DynamoDB에 직접 저장
- TTL 5분 (자동 삭제)

#### 5단계: Executor → Bybit
**처리 방식**:
- 5초마다 DynamoDB 스캔 (StatusIndex 사용)
- `active` 상태 포지션만 조회
- 진입 조건 확인 후 주문 실행

---

## 인프라 구성

### AWS 리소스

#### 1. VPC 및 네트워크

- **VPC ID**: `vpc-07a289adc49898e52`
- **서브넷**: 기존 퍼블릭 서브넷 사용
- **보안 그룹**: ECS 태스크용 (아웃바운드 전체 허용)

#### 2. ECS Cluster
- **이름**: `crypto-backtest-cluster`
- **타입**: Fargate (서버리스)
- **리전**: `ap-northeast-2` (서울)

**서비스 구성**:
| 서비스 | 실행 방식 | 인스턴스 | CPU | 메모리 | 비용/월 |
|--------|-----------|----------|-----|--------|---------|
| Scanner | EventBridge (1시간) | 1 태스크 | 512 | 1024 | ~$2 |
| Analyzer | ECS Service | 1~10 (Auto) | 1024 | 2048 | ~$30 |
| Selector | EventBridge (1분) | 1 태스크 | 256 | 512 | ~$5 |
| Finder | ECS Service | 1~5 (Auto) | 512 | 1024 | ~$15 |
| Executor | ECS Service | 1 (고정) | 256 | 512 | ~$8 |
| **합계** | - | - | - | - | **~$60** |

#### 3. DynamoDB 테이블

**crypto-backtest-scan-results**:
- **용도**: 스캔 결과 저장
- **PK**: `scan_id` (String)
- **SK**: `symbol` (String)
- **TTL**: 24시간
- **예상 용량**: ~1 KB/항목 × 19개 × 24회/일 = ~456 KB/일

**crypto-backtest-backtest-results**:
- **용도**: 백테스팅 결과 저장
- **PK**: `symbol` (String)
- **SK**: `backtest_id` (String)
- **인덱스**: `TimeframeIndex` (timeframe)
- **TTL**: 7일
- **예상 용량**: ~2 KB/항목 × 152개 × 24회/일 = ~7.3 MB/일

**crypto-backtest-trading-positions**:
- **용도**: 실시간 포지션 저장
- **PK**: `symbol` (String)
- **SK**: `signal_timestamp` (Number)
- **GSI**: `StatusIndex` (status)
- **TTL**: 5분 (자동 삭제)
- **예상 용량**: ~3 KB/항목 × 20개 동시 = ~60 KB

**비용 추정**:
- 읽기: ~1,000 RCU/일 = ~$0.50/월
- 쓰기: ~500 WCU/일 = ~$2.50/월
- 스토리지: ~1 GB = ~$0.25/월
- **합계**: ~$3.25/월

#### 4. Amazon MQ (RabbitMQ)
- **브로커 이름**: `crypto-trading-broker`
- **타입**: `mq.t3.micro` (개발/테스트용)
- **엔진**: RabbitMQ 3.11
- **배포 모드**: Single-instance
- **스토리지**: 20 GB EBS
- **포트**: 5671 (SSL), 443 (Web Console)
- **비용**: ~$18/월

**큐 구성**:
- `scan-results`: Durable, 메시지 TTL 1시간
- `backtest-results`: Durable, 메시지 TTL 1시간
- `trading-signals`: Durable, 메시지 TTL 30분

#### 5. ECR (Container Registry)
- **리포지토리**: 5개 (서비스별)
- **이미지 크기**: ~500 MB/개
- **스토리지**: ~2.5 GB
- **비용**: ~$0.25/월

#### 6. Secrets Manager
- **시크릿**: 3개
  - `crypto-backtest/bybit-api-key`: Bybit API Key
  - `crypto-backtest/bybit-api-secret`: Bybit API Secret
  - `crypto-backtest/bybit-testnet`: Testnet 여부 (False)
- **비용**: ~$1.20/월 (3개 × $0.40)

#### 7. EventBridge (CloudWatch Events)
**스케줄 규칙**:

**Scanner Schedule**:
- **규칙 이름**: `crypto-backtest-scanner-schedule`
- **스케줄**: `rate(1 hour)` (1시간마다)
- **타겟**: ECS Task (Scanner)
- **설명**: 거래 가능한 코인 스캔 및 백테스팅 작업 생성

**Selector Schedule**:
- **규칙 이름**: `crypto-backtest-selector-schedule`
- **스케줄**: `rate(1 minute)` (1분마다)
- **타겟**: ECS Task (Selector)
- **설명**: 백테스팅 결과에서 최적 전략 선택 및 진입 신호 생성

**실행 흐름**:
```
EventBridge Rule (1시간) 
  → ECS RunTask (Scanner)
  → RabbitMQ 메시지 발행
  → Analyzer Service (상시 실행 중) 메시지 소비

EventBridge Rule (1분)
  → ECS RunTask (Selector)
  → DynamoDB 조회 및 분석
  → RabbitMQ 메시지 발행
  → Finder Service (상시 실행 중) 메시지 소비
```

**IAM 역할**:
- **역할 이름**: `crypto-backtest-eventbridge-ecs`
- **권한**: 
  - `ecs:RunTask` (Scanner, Selector 태스크 실행)
  - `iam:PassRole` (태스크 실행/태스크 역할 전달)

**비용**: 무료 (월 100만 이벤트까지 무료, 실제 사용량 ~50,000 이벤트/월)

### 총 인프라 비용
- **ECS Fargate**: ~$60/월
- **DynamoDB**: ~$3.25/월
- **Amazon MQ**: ~$18/월
- **ECR**: ~$0.25/월
- **Secrets Manager**: ~$1.20/월
- **EventBridge**: 무료
- **데이터 전송**: ~$5/월
- **합계**: **~$88/월**

---

## 핵심 전략

### 진입 전략: Fibonacci + Bollinger Bands

#### 1. 멀티 타임프레임 피보나치
**계산 방법**:
```python
# 각 타임프레임별 고점/저점 찾기
timeframes = ['1D', '4h', '1h', '15m']

for tf in timeframes:
    # 해당 기간의 캔들 데이터 로드
    candles = get_klines_for_days(symbol, tf, days)
    
    # 고점/저점
    high = candles['high'].max()
    low = candles['low'].min()
    diff = high - low
    
    # 피보나치 레벨
    levels = {
        '0.0': low,
        '0.236': low + diff * 0.236,
        '0.382': low + diff * 0.382,
        '0.5': low + diff * 0.5,
        '0.618': low + diff * 0.618,
        '0.786': low + diff * 0.786,
        '1.0': high
    }
```

**지지/저항 판단**:
- 현재가가 피보나치 레벨 ±2% 이내면 "근처"로 판단
- 여러 타임프레임의 레벨이 겹치면 강한 지지/저항

#### 2. 볼린저 밴드
**설정**:
- 기간: 20
- 표준편차: 2.0

**계산**:
```python
middle_band = SMA(close, 20)
std = STD(close, 20)
upper_band = middle_band + (2 * std)
lower_band = middle_band - (2 * std)

# 현재 위치 (-1 ~ +1)
bb_position = (close - lower_band) / (upper_band - lower_band) * 2 - 1
```

**진입 조건**:
- **롱**: `bb_position < -0.7` (하단 근처)
- **숏**: `bb_position > 0.7` (상단 근처)

#### 3. RSI (Relative Strength Index)
**설정**:
- 기간: 14

**진입 조건**:
- **롱**: RSI < 35 (과매도)
- **숏**: RSI > 65 (과매수)

#### 4. BTC 추세 필터
**계산**:
```python
# BTC 4시간봉 20개 (최근 3.3일)
btc_candles = get_klines('BTCUSDT', '240', 20)

# 가격 변화율
price_change = (current_price - first_price) / first_price * 100

# 추세 판단
if price_change > 2:
    trend = 'BULLISH'
elif price_change < -2:
    trend = 'BEARISH'
else:
    trend = 'NEUTRAL'
```

**필터링**:
- **롱**: BTC 추세가 BULLISH 또는 NEUTRAL
- **숏**: BTC 추세가 BEARISH 또는 NEUTRAL

#### 5. 펀딩비 역추세
**로직**:
```python
funding_rate = get_funding_rate(symbol)

# 펀딩비가 양수 → 롱 포지션이 많음 → 숏 진입 유리
# 펀딩비가 음수 → 숏 포지션이 많음 → 롱 진입 유리

if position_type == 'LONG' and funding_rate < 0:
    confidence += 5  # 역추세
elif position_type == 'SHORT' and funding_rate > 0:
    confidence += 5  # 역추세
```

### 손절/익절 전략

#### 손절가 (Stop Loss)
**계산 방법**:
```python
# ATR 기반 (Average True Range)
atr = calculate_atr(candles, period=14)
atr_multiplier = 1.5

if position_type == 'LONG':
    stop_loss = entry_price - (atr * atr_multiplier)
else:  # SHORT
    stop_loss = entry_price + (atr * atr_multiplier)

# tickSize에 맞춰 반올림
stop_loss = round_price(stop_loss, tick_size, price_decimals)
```

**손실 한도**: 포지션 크기의 -1.0% (약 $1)

#### 익절가 (Take Profit)
**계산 방법**:
```python
# 손익비 2:1 고정
profit_distance = abs(entry_price - stop_loss) * 2.0

if position_type == 'LONG':
    take_profit = entry_price + profit_distance
else:  # SHORT
    take_profit = entry_price - profit_distance

# tickSize에 맞춰 반올림
take_profit = round_price(take_profit, tick_size, price_decimals)
```

**수익 목표**: 포지션 크기의 +2.0% (약 $2)

### 리스크 관리

#### 포지션 크기
- **고정**: $100/포지션
- **레버리지**: 10x
- **실제 마진**: $10/포지션

#### 동시 포지션 수
- **최대**: 계정 잔고에 따라 자동 조절
- **예시**: $1,000 잔고 → 최대 100개 포지션 (이론상)
- **실제**: 진입 조건이 까다로워 동시에 5~10개 정도

#### 손실 한도
- **포지션당**: -$1 (1%)
- **일일**: 제한 없음 (자동 손절로 관리)
- **계정**: -20% 도달 시 수동 중단

---

## 배포 및 운영

### 배포 프로세스

#### 1. 인프라 배포 (최초 1회)
```bash
cd infrastructure/terraform
terraform init
terraform plan
terraform apply
```

**생성 리소스**:
- ECS Cluster
- DynamoDB 테이블 3개
- Amazon MQ 브로커
- ECR 리포지토리 5개
- IAM 역할 및 정책

#### 2. Docker 이미지 빌드 및 푸시
```bash
./scripts/build-and-push.sh
```

**프로세스**:
1. 각 서비스별 Dockerfile 빌드
2. ECR 로그인
3. 이미지 태깅 (latest)
4. ECR에 푸시

#### 3. ECS 서비스 업데이트
```bash
./scripts/update-services.sh
```

**프로세스**:
1. 새 태스크 정의 등록
2. 서비스 업데이트 (force-new-deployment)
3. 기존 태스크 종료
4. 새 태스크 시작

**배포 시간**: 서비스당 ~2분 (총 ~10분)

#### 4. 전체 시스템 배포
```bash
./scripts/deploy_full_system.sh
```

**프로세스**:
1. 인프라 배포 (필요시)
2. Docker 이미지 빌드 및 푸시
3. ECS 서비스 업데이트
4. 헬스 체크

### 환경 변수 관리

#### .env 파일
```bash
# Bybit API
BYBIT_API_KEY=your_api_key
BYBIT_API_SECRET=your_api_secret
BYBIT_TESTNET=False

# AWS
AWS_REGION=ap-northeast-2
AWS_ACCOUNT_ID=081041735764

# DynamoDB
DYNAMODB_SCAN_TABLE=crypto-backtest-scan-results
DYNAMODB_BACKTEST_TABLE=crypto-backtest-backtest-results
DYNAMODB_POSITIONS_TABLE=crypto-backtest-trading-positions

# RabbitMQ
RABBITMQ_HOST=b-xxx.mq.ap-northeast-2.amazonaws.com
RABBITMQ_PORT=5671
RABBITMQ_USER=admin
RABBITMQ_PASS=your_password

# Trading
POSITION_SIZE=100.0
LEVERAGE=10
```

#### Secrets Manager
- ECS 태스크 정의에서 환경 변수로 주입
- 민감한 정보 (API 키, 비밀번호) 보호

### 로그 관리

#### CloudWatch Logs
- **로그 그룹**: `/ecs/crypto-trading/{service-name}`
- **보존 기간**: 7일
- **로그 스트림**: 태스크 ID별

**로그 확인**:
```bash
# 최근 로그 확인
aws logs tail /ecs/crypto-trading/executor-service --follow

# 특정 시간대 로그
aws logs filter-log-events \
  --log-group-name /ecs/crypto-trading/executor-service \
  --start-time 1702809600000 \
  --end-time 1702813200000
```

### 모니터링 대시보드

#### 주요 메트릭
1. **서비스 상태**
   - 실행 중인 태스크 수
   - CPU/메모리 사용률
   - 네트워크 I/O

2. **메시지 큐**
   - 큐 길이
   - 메시지 처리 속도
   - 에러율

3. **트레이딩**
   - 활성 포지션 수
   - 일일 거래 횟수
   - 승률 및 손익

4. **DynamoDB**
   - 읽기/쓰기 용량
   - 스로틀링 발생 여부
   - 테이블 크기

#### 알람 설정
- ECS 태스크 중단 시
- 메시지 큐 적체 (>100개)
- DynamoDB 스로틀링
- API 에러율 > 5%

---

## 트러블슈팅

### 일반적인 문제

#### 1. 레버리지 설정 에러 (110043)
**증상**: `leverage not modified (ErrCode: 110043)`
**원인**: 레버리지가 이미 설정되어 있음
**해결**: 에러 무시하고 계속 진행 (코드에 이미 반영됨)

#### 2. DynamoDB Float 에러
**증상**: `Float types are not supported`
**원인**: DynamoDB는 float를 지원하지 않음
**해결**: `convert_floats_to_decimal()` 함수 사용

#### 3. 소수점 불일치
**증상**: 주문 실패 (가격/수량 형식 오류)
**원인**: tickSize/qtyStep에 맞지 않는 소수점
**해결**: `get_instrument_info()`로 정확한 소수점 조회 후 반올림

#### 4. 중복 진입
**증상**: 같은 코인에 여러 포지션 진입
**원인**: Finder와 Executor 간 동기화 부족
**해결**: 
- Finder: 최근 5분 이내 포지션 확인
- Executor: Bybit API로 실제 오픈 포지션 확인

#### 5. 잔고 부족
**증상**: 주문 실패 (insufficient balance)
**원인**: 사용 가능 마진 부족
**해결**: 
- `totalAvailableBalance` 사용 (Unified Trading Account)
- 오픈 포지션의 사용 마진 계산 후 차감

### 디버깅 팁

#### 로그 레벨 조정
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

#### 로컬 테스트
```bash
# 환경 변수 로드
source .env

# 단일 서비스 실행
python services/scanner/scanner_service.py
```

#### RabbitMQ 모니터링
- Web Console: `https://{broker-host}:443`
- 큐 길이, 메시지 처리 속도 확인

#### DynamoDB 쿼리
```bash
# 활성 포지션 조회
aws dynamodb query \
  --table-name crypto-backtest-trading-positions \
  --index-name StatusIndex \
  --key-condition-expression "#status = :status" \
  --expression-attribute-names '{"#status":"status"}' \
  --expression-attribute-values '{":status":{"S":"active"}}'
```

---

## 향후 개선 사항

### 단기 (1개월)
- [ ] 포지션 청산 로직 구현 (TP/SL 도달 시)
- [ ] 실시간 성과 대시보드 구축
- [ ] 알람 시스템 구축 (SNS/Slack)
- [ ] 백테스팅 결과 시각화

### 중기 (3개월)
- [ ] 머신러닝 기반 신호 강화
- [ ] 멀티 거래소 지원 (Binance, OKX)
- [ ] 동적 포지션 크기 조절
- [ ] 고급 리스크 관리 (포트폴리오 레벨)

### 장기 (6개월)
- [ ] 자동 파라미터 최적화
- [ ] 소셜 트레이딩 기능
- [ ] 모바일 앱 개발
- [ ] 클라우드 비용 최적화 (Spot Instances)

---

## 참고 문서

- [Bybit API 문서](https://bybit-exchange.github.io/docs/v5/intro)
- [AWS ECS 문서](https://docs.aws.amazon.com/ecs/)
- [DynamoDB 문서](https://docs.aws.amazon.com/dynamodb/)
- [RabbitMQ 문서](https://www.rabbitmq.com/documentation.html)
- [Terraform AWS Provider](https://registry.terraform.io/providers/hashicorp/aws/latest/docs)

---

**문서 버전**: 1.0  
**최종 업데이트**: 2025-12-17  
**작성자**: Kiro AI Assistant
